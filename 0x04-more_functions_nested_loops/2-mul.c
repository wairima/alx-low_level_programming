#include "main.h"
#include <stdio.h>
/**
  * mul - multiplies the two integers provided
  * @a: The first integer
  * @b: The second integer
  * Return: Always 0 success
  */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
